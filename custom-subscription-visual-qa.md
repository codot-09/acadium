Custom admin subscription release visual QA: desktop /admin route rendered safely through the existing secure-session loading state at 1280x720. Mobile 390x844 retained the existing responsive admin login composition with readable labels, full-width touch-friendly fields/button, no horizontal overflow, and no changes to the prior login behavior. The activation form is covered by AdminPage component interaction tests because unauthenticated visual preview cannot access the admin-only subscriptions tab.
